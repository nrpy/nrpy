GPU Info:
""""
 # 1. Hardware Model & PCI ID
lspci -vnn | grep -i vga -A 12

# 2. Driver and Runtime CUDA version
nvidia-smi

# 3. Compiler Version (Toolkit version)
nvcc --version

# 4. Location of the Compiler
which nvcc

# 5. Host Compiler Version (Crucial for CUDA compatibility)
gcc --version
05:00.0 VGA compatible controller [0300]: NVIDIA Corporation GA102 [GeForce RTX 3080 Lite Hash Rate] [10de:2216] (rev a1) (prog-if 00 [VGA controller])
        Subsystem: Lenovo GA102 [GeForce RTX 3080 Lite Hash Rate] [17aa:c891]
        Flags: bus master, fast devsel, latency 0, IRQ 97, IOMMU group 16
        Memory at fb000000 (32-bit, non-prefetchable) [size=16M]
        Memory at 7fe0000000 (64-bit, prefetchable) [size=256M]
        Memory at 7ff0000000 (64-bit, prefetchable) [size=32M]
        I/O ports at e000 [size=128]
        Expansion ROM at fc000000 [virtual] [disabled] [size=512K]
        Capabilities: <access denied>
        Kernel driver in use: nvidia
        Kernel modules: nvidiafb, nouveau, nvidia_drm, nvidia

05:00.1 Audio device [0403]: NVIDIA Corporation GA102 High Definition Audio Controller [10de:1aef] (rev a1)
Fri Feb 27 00:12:08 2026       
+---------------------------------------------------------------------------------------+
| NVIDIA-SMI 535.288.01             Driver Version: 535.288.01   CUDA Version: 12.2     |
|-----------------------------------------+----------------------+----------------------+
| GPU  Name                 Persistence-M | Bus-Id        Disp.A | Volatile Uncorr. ECC |
| Fan  Temp   Perf          Pwr:Usage/Cap |         Memory-Usage | GPU-Util  Compute M. |
|                                         |                      |               MIG M. |
|=========================================+======================+======================|
|   0  NVIDIA GeForce RTX 3080        Off | 00000000:05:00.0  On |                  N/A |
| 30%   29C    P8              10W / 320W |    415MiB / 10240MiB |      0%      Default |
|                                         |                      |                  N/A |
+-----------------------------------------+----------------------+----------------------+
                                                                                         
+---------------------------------------------------------------------------------------+
| Processes:                                                                            |
|  GPU   GI   CI        PID   Type   Process name                            GPU Memory |
|        ID   ID                                                             Usage      |
|=======================================================================================|
|    0   N/A  N/A      1722      G   /usr/lib/xorg/Xorg                          160MiB |
|    0   N/A  N/A     24952      G   /usr/bin/gnome-shell                         38MiB |
|    0   N/A  N/A     28194      G   /usr/bin/nautilus                            15MiB |
|    0   N/A  N/A     37699      G   ...cess-track-uuid=3190708988185955192       65MiB |
|    0   N/A  N/A     38272      G   /usr/share/code/code                        120MiB |
+---------------------------------------------------------------------------------------+
nvcc: NVIDIA (R) Cuda compiler driver
Copyright (c) 2005-2023 NVIDIA Corporation
Built on Fri_Jan__6_16:45:21_PST_2023
Cuda compilation tools, release 12.0, V12.0.140
Build cuda_12.0.r12.0/compiler.32267302_0
/usr/bin/nvcc
gcc (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0
Copyright (C) 2023 Free Software Foundation, Inc.
This is free software; see the source for copying conditions.  There is NO
warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.

""""


BUNDLE_CAPACITY Vs chunk_size:
""""
--- STRICT ARCHITECTURAL RULE: MEMORY STRIDING VS. THREAD BOUNDING ---
When operating on batched data within the Split-Pipeline architecture, you MUST strictly separate the concepts of Memory Striding and Thread Bounding to prevent buffer overruns and zero-state data corruption on the final execution batch.

* VRAM Scratchpad Allocation: Arrays like `d_f_bundle`, `d_metric_bundle`, and `d_k_bundle` are statically allocated based on the maximum hardware limit (`BUNDLE_CAPACITY = 32768`).
* MEMORY STRIDING (MUST USE BUNDLE_CAPACITY): 
  When defining Structure of Arrays (SoA) access macros, the offset multiplier MUST ALWAYS be `BUNDLE_CAPACITY`. The physical layout of the VRAM does not shrink on the final batch.
  CORRECT: `#define IDX_F(comp, ray_id) ((comp) * BUNDLE_CAPACITY + (ray_id))`
  FATAL: `#define IDX_F(comp, ray_id) ((comp) * chunk_size + (ray_id))`
  
* THREAD BOUNDING & TRANSFERS (MUST USE chunk_size):
  The variable `chunk_size` represents the actual physical number of active photons in the current batch (which will be less than `BUNDLE_CAPACITY` on the final remainder batch). 
  1. Thread Guards: You MUST use `chunk_size` to prevent phantom threads from executing: `if (i >= chunk_size) return;`
  2. Memory Transfers: You MUST use `chunk_size` for the payload size in `cudaMemcpy` to avoid host-array buffer overruns: `sizeof(double) * chunk_size`. However, the device pointer offset must still respect the VRAM stride (e.g., `d_f_bundle + (comp * BUNDLE_CAPACITY)`).
""""





Plan for batch integrator:
""""
--- STRICT ARCHITECTURAL RULE: THE SPLIT-PIPELINE MANDATE ---
To prevent register pressure from exceeding the 255-register limit of the RTX 3080 during complex numerical spacetime evaluations, you MUST strictly enforce a "Split-Pipeline" architecture.
* Do NOT attempt to "fuse" the RKF45 integration kernels. 
* Do NOT use `__shared__` memory for intermediate data between RKF45 stages.
* ALL intermediate data (metrics, connections, derivatives $k$) MUST be persisted in Global VRAM via flattened Structure of Arrays (SoA) "Scratchpad Bundles".

--- MASTER DATA STRUCTURES (C-STRUCTS) ---
The following structures define the memory layout. The master photon structs are generated by `set_initial_conditions_kernel.py`. Use these exact types and variable names.

#define BUNDLE_CAPACITY 32768 // Maximum photons processed per batch (VRAM limit protection)

typedef enum { WINDOW_EVENT, SOURCE_EVENT } event_type_t;

typedef enum {
    TERMINATION_TYPE_CELESTIAL_SPHERE, // 0: Escaped to infinity
    TERMINATION_TYPE_SOURCE_PLANE,     // 1: Hit source plane
    FAILURE_PT_TOO_BIG,                // 2: Unbounded p_t
    FAILURE_RKF45_REJECTION_LIMIT,     // 3: Adaptive step-size rejected too many times
    FAILURE_T_MAX_EXCEEDED,            // 4: Exceeded max physical time
    FAILURE_SLOT_MANAGER_ERROR,        // 5: TimeSlotManager failure
    TERMINATION_TYPE_FAILURE,          // 6: Generic numerical failure
    ACTIVE,                             // 7: Undergoing integration
    REJECTED,                          // 8: Photon rkf45 step was rejected and step
} termination_type_t;

typedef struct {
    termination_type_t termination_type;
    double y_w; double z_w; // Observer window intersections
    double y_s; double z_s; // Source plane intersections
    double final_theta; double final_phi;
    double L_w; double t_w; // Window affine and time
    double L_s; double t_s; // Source affine and time
} __attribute__((packed)) blueprint_data_t;

// Flattened SoA Struct (Master Storage)
typedef struct {
    double *f;
 double *f_p; 
double *f_p_p; // State vector history
    double *affine_param;
 double *affine_param_p; 
double *affine_param_p_p;
    double *h; 
    termination_type_t *status; 
    int *rejection_retries; 
    bool *on_positive_side_of_window_prev; 
    bool *on_positive_side_of_source_prev;
    bool *source_event_found; double *source_event_lambda; double *source_event_f_intersect;
    bool *window_event_found; double *window_event_lambda; double *window_event_f_intersect;
} PhotonStateSoA;



// Time Slot Manager Structure & API
typedef struct {
  double t_min;               
  double t_max;               
  double delta_t_slot;        
  int num_slots;              
  long int max_capacity;      
  long int *photon_next_ptrs; 
  long int *slot_heads;       
  long int *slot_counts;      
} TimeSlotManager;

// TSM Function Signatures (Assume these are available via include)
static inline void slot_manager_init(TimeSlotManager *tsm, double t_min, double t_max, double delta_t_slot, long int num_rays);
static inline void slot_manager_free(TimeSlotManager *tsm);
static inline int slot_get_index(const TimeSlotManager *tsm, double t);
static inline void slot_add_photon(TimeSlotManager *tsm, int slot_idx, long int photon_idx);
static inline void slot_remove_chunk(TimeSlotManager *tsm, int slot_idx, long int *chunk_buffer, long int chunk_size);


// CONSTANT MEMORY DICTIONARY
// Access these global parameters in device code via `d_commondata.variable_name`
typedef struct __commondata_struct__ {
  REAL camera_pos_x; REAL camera_pos_y; REAL camera_pos_z;
  REAL window_center_x; REAL window_center_y; REAL window_center_z;
  REAL window_width; REAL window_height;
  REAL window_up_vec_x; REAL window_up_vec_y; REAL window_up_vec_z;
  REAL source_plane_center_x; REAL source_plane_center_y; REAL source_plane_center_z;
  REAL source_plane_normal_x; REAL source_plane_normal_y; REAL source_plane_normal_z;
  REAL source_r_min; REAL source_r_max;
  REAL source_up_vec_x; REAL source_up_vec_y; REAL source_up_vec_z;
  REAL numerical_initial_h; 
  REAL rkf45_h_max; REAL rkf45_h_min; 
  REAL rkf45_error_tolerance; REAL rkf45_absolute_error_tolerance; REAL rkf45_safety_factor;
  REAL p_t_max; REAL r_escape; REAL t_integration_max;
  REAL slot_manager_delta_t; REAL slot_manager_t_min; REAL t_start;
  int rkf45_max_retries; int scan_density;
} commondata_struct;

--- PIPELINE EXECUTION SEQUENCE (STRICT ORDERING) ---
Implement the orchestrator workflow sequentially:

1. HOST & DEVICE ALLOCATION:
   - Allocate `PhotonStateSoA` in Host Pinned Memory.
   - Allocate Bridge arrays (f, h, status) of size `BUNDLE_CAPACITY` in Host Pinned Memory.
   - Allocate 1D VRAM Scratchpad arrays (f_bundle, f_temp, metric, connection, k_bundle).
   - `k_bundle` must fit 6 stages: size `6 * 9 * BUNDLE_CAPACITY`.
   - Initialize Host `TimeSlotManager` (TSM).

2. INITIALIZATION PHASE:
   - Launch `set_initial_conditions_kernel`.
   - Launch `interpolation_kernel` (Skip connections - pass NULL).
   - Launch `p0_reverse_kernel`.
   - D2H Sync -> Seed photons into TSM bins.

3. TEMPORAL LOOP (The Engine):
   - Loop over Time Bins (`slot_idx`).
   - While `tsm.slot_counts[slot_idx] > 0`:
       A. Bridge Transfer: `slot_remove_chunk` to get rays. H2D copy to Scratchpad VRAM.
       B. Split RKF45 Loop: `for (int stage = 1; stage <= 6; stage++)`
          - `interpolation_kernel` (Calculate Metric & Connections)
          - `calculate_ode_rhs_kernel` (Calculate $\dot{f}$, write to offset in `k_bundle`)
          - `rkf45_stage_update_kernel` (Update `f_temp`)
       C. Finalization: 
          - `rkf45_finalize_and_control` (Adapt $h$, calculate errors).
          - `event_detection_manager_kernel` (Geometric intersection checks).
       D. Feedback & Re-binning:
          - D2H copy statuses and times.
          - Iterate chunk: Retire (TERMINATED), Retry current bin (REJECTED), or advance to future bin (ACCEPTED).

4. CLEANUP:
   - D2H copy `d_results_buffer` to Host.
   - Free all Pinned RAM and VRAM. Destroy TSM and Streams.
""""








STRICT ARCHITECTURAL RULE: THE SPLIT-PIPELINE MANDATE:
"""
Singularity-Axiom: Modular Physics Pipeline

MISSION:
Decompose the fused RKF45 integration into a "Single-Instruction Multiple-Data" (SIMD) batch pipeline. This prevents register pressure from exceeding the 255-register limit of the RTX 3080 (sm_86) during complex numerical spacetime evaluations.

ARCHITECTURAL FLOW:
The orchestrator must launch the following three kernels sequentially for each of the 6 RKF45 stages:

    Batch Interpolation Kernel (placeholder_interpolation):
    Input: Current state bundle $f^\mu$ (VRAM).
    Output: Symmetric metric $g_{\mu\nu}$ and Christoffel symbols $\Gamma^\alpha_{\beta\gamma}$ stored in VRAM-resident bundle arrays.
    Optimization: Utilize BHAH_HD_FUNC for zero-overhead tensor evaluation.
    Goal: Minimize VRAM stalls by ensuring coalesced memory access across the bundle.

    Batch RHS Kernel (calculate_ode_rhs):
    Input: State bundle $f^\mu$ + Interpolated tensor bundle (VRAM).
    Output: Derivative bundle $\dot{f}$ (the $k$-array).
    Constraint: Output MUST be written to Global VRAM. Do NOT use Shared Memory, as this data must persist for the subsequent Update kernel.
    Logic: Solve the Geodesic Equation: $\frac{d^2x^\mu}{d\lambda^2} + \Gamma^\mu_{\alpha\beta}\frac{dx^\alpha}{d\lambda}\frac{dx^\beta}{d\lambda} = 0$.

    Adaptive Stage-Update Kernel (rkf45_stage_update):
    Input: int stage, current $f_{start}$, $\dot{f}$ bundle, and step size $h$.
    Logic: Use a switch(stage) block to apply specific RKF45 coefficients (e.g., Stage 3 uses $3/32 \cdot k_1 + 9/32 \cdot k_2$).
    Memory: Update the f_temp bundle in VRAM for the next interpolation cycle.

CONSTRAINTS:

Persistence: 
All intermediate data (metrics, connections, and RHS derivatives) must be stored in strided Structure of Arrays (SoA) bundles to protect the 10GB VRAM limit.

No Synchronization: 
Do not synchronize the GPU between the Interpolation, RHS, and Update kernels unless the hardware requires it; allow the Compute Command Processor to schedule them as a continuous stream.

Metadata: 
All kernels must access global tolerances and physics constants via Constant Memory (d_commondata).
"""

Context Prompt:
"""
Singularity-Axiom: Architectural Manifesto & System Context

PROJECT GOAL:
The ultimate objective of Project Singularity-Axiom is to perform relativistic ray tracing on **Numerical Spacetimes** (e.g., discrete BSSN/ADM data slices), not just analytic metrics (like Kerr-Schild).
This requirement dictates the entire software architecture.

THE "REGISTER BLEEDING" CONSTRAINT:
* Target Hardware: NVIDIA RTX 3080 (Ampere / sm_86).
* Hard Limit: 255 Registers per thread.
* The Problem: High-order numerical interpolation of 4D spacetime metrics requires massive register pressure.
* The Failure Mode: A "Fused Kernel" approach (combining Interpolation + ODE RHS + RKF45 Stepper) forces the compiler to spill registers to Local Memory (L1/L2 cache thrashing), causing performance to collapse by orders of magnitude.

THE "SPLIT-PIPELINE" MANDATE:
To solve Register Bleeding, we strictly enforce a **Split-Pipeline Architecture**.
We trade VRAM Bandwidth for Register Stability.
The integration step is decomposed into discrete, specialized kernels that communicate via VRAM "Scratchpad Bundles" ($d\_metric$, $d\_connection$, $d\_k$, $d\_f\_temp$):

1. Batch Interpolation Kernel: Calculates Geometry ($g_{\mu\nu}, \Gamma$) $\to$ Saves to VRAM.
2. Batch RHS Kernel: Reads Geometry $\to$ Calculates Derivatives ($k$) $\to$ Saves to VRAM.
3. Stage Update Kernel: Reads Derivatives $\to$ Updates State ($f_{temp}$).

ANTI-PATTERN WARNING (DO NOT VIOLATE):
* DO NOT suggest merging or "fusing" these kernels for "optimization."
* DO NOT attempt to use `__shared__` memory or Registers to persist data between these stages (the context is flushed between kernel launches).
* DO NOT inline the Metric Interpolation logic into the RKF45 stepper.

Future LLMs reading this context must understand that the **separation of concerns via VRAM Scratchpads is intentional and mandatory** to support the future ingestion of large-scale Numerical Relativity datasets.
"""


Info for Kernel Generation:
"""
--- THE 7 DISCRETE KERNELS (C-SIGNATURES FOR ORCHESTRATOR) ---
The orchestrator must invoke these exact signatures. All grid configurations (blocks/threads) are handled automatically by the wrapper macros inside the kernels, EXCEPT where explicit parameters are listed.

// 1. INITIALIZATION: Sets starting conditions on the Host.
void set_initial_conditions_kernel_{spacetime_name}(
    const commondata_struct *restrict commondata, 
    long int num_rays, 
    PhotonStateSoA *restrict all_photons, 
    double window_center_out[3], 
    double n_x_out[3], double n_y_out[3], double n_z_out[3]
);

// 2. INTERPOLATION: Calculates g_mu_nu and Gamma (Pass NULL for connection_bundle in Phase 1).
void interpolation_kernel_{spacetime_name}(
    const double *restrict d_f_bundle, 
    double *restrict d_metric_bundle, 
    double *restrict d_connection_bundle, 
    const int chunk_size
);

// 3. P0 REVERSE: Solves Hamiltonian for p^t (Initialization Phase).
void p0_reverse_kernel(
    double *restrict d_f_bundle, 
    const double *restrict d_metric_bundle, 
    const int chunk_size
);

// 4. RHS CALCULATOR: Calculates k-derivatives for the current stage.
void calculate_ode_rhs_kernel(
    const double *restrict d_f_temp_bundle, 
    const double *restrict d_metric_bundle, 
    const double *restrict d_connection_bundle, 
    double *restrict d_k_bundle, 
    const int stage, 
    const int chunk_size
);

// 5. STAGE UPDATE: Applies Butcher tableau to generate f_temp.
void rkf45_stage_update(
    const double *restrict d_f_start, 
    const double *restrict d_k_bundle, 
    const double *restrict d_h, 
    const int stage, 
    const int chunk_size, 
    double *restrict d_f_temp
);

// 6. FINALIZE & CONTROL: Evaluates error, adapts h, updates f_start if accepted.
void rkf45_finalize_and_control(
    double *restrict d_f_persistent, 
    const double *restrict d_f_start, 
    const double *restrict d_k_bundle, 
    double *restrict d_h, 
    termination_type_t *restrict d_status, 
    double *restrict d_affine, 
    int *restrict d_retries, 
    const long int chunk_size
);

// 7. EVENT DETECTION: Checks geometric intersections and shifts VRAM history.
void event_detection_manager_kernel(
    const double *restrict d_f_bundle, 
    double *restrict d_f_prev_bundle, 
    double *restrict d_f_pre_prev_bundle, 
    blueprint_data_t *restrict d_results_buffer, 
    termination_type_t *restrict d_status_bundle, 
    bool *restrict d_on_pos_window_prev, 
    bool *restrict d_on_pos_source_prev, 
    const int chunk_size
);
"""

Event detection Pipeline:
"""
MISSION: Implement a register-resident Event Detection Micro-System that executes entirely on the GPU within a "Streaming Bundle" architecture. The system must detect transitions across geometric planes (Observer Window and Source Plane) by evaluating sign changes in the scalar product of the photon's trajectory relative to the target plane normal.

HARDWARE TARGET: NVIDIA RTX 3080 (Ampere / sm_86).

VRAM & MEMORY ARCHITECTURE:
* Persistent Tracking: Use Pinned (Host) and Device (VRAM) boolean buffers (on_pos_window_prev, on_pos_source_prev) to track the "previous side" of each plane across discrete integration steps.

* Register Residence: All logic, interpolation, and basis reconstructions must occur within the thread-local f_local[9] registers to respect the 255-register hardware limit.

* Constant Memory: Access all geometric parameters (camera positions, source normals, and radial bounds) via the 64KB d_commondata cache.

THE EVENT DETECTION PIPELINE (Thread-Level Execution):

1. Coordinate Hydration:
   * Load current state $f^\mu_{n}$ and history $f^\mu_{n-1}$, $f^\mu_{n-2}$ from the VRAM bundle.
   * Retrieve persistent side-flags from the bundle buffer.

2. Sign-Change Detection:
   * Evaluate the plane equation $V = n_i x^i - d$ for the current step.
   * Compare current side against prev flag using a stability threshold of $val > 10^{-10}$.

3. High-Precision Reconstruction (On Detection):
   * Root-Finding: If a sign change is detected, invoke find_event_time_and_state.
   * Interpolation: Utilize Quadratic Interpolation (Newton form) for the affine parameter $\lambda$ and Lagrange Interpolation for the 9-component state reconstruction at the exact intersection boundary.

4. Geometric Filtering & Termination:
   * Observer Window: Project the 3D intersection into the local orthonormal basis $(y_w, z_w)$. If within bounds, update the results_buffer (blueprint) without terminating the ray.
   * Source Emission Plane: Project into local coordinates $(y_s, z_s)$. Apply radial filtering ($r_{min} < r < r_{max}$). If valid, update the blueprint and set status to TERMINATION_TYPE_SOURCE_PLANE.

5. State Persistence:
   * Update the prev side-flags in VRAM to the current state for the next integration cycle.

INFRASTRUCTURE CONSTRAINTS:

* Metaprogramming: Use utilities.generate_kernel_and_launch_code with cfunc_decorators="__global__".

* Inlining: All helper functions must be decorated with BHAH_HD_FUNC for zero-overhead inlining into the master kernel.

* Data Integrity: Ensure the results_buffer is copied Host-to-Device before the kernel launch to prevent overwriting previous successful detections.
"""



COMMENTING STANDARDS FOR SCRIPT GENERATION. (ONLY NEED TO WORRY ABOUT IT ASKED TO GENERATE SCRIPT)
""""
Role and Objective: You are an expert computational physicist and High-Performance Computing (HPC) software engineer. Your task is to refactor, format, and exhaustively comment the provided Python metaprogramming scripts that generate C code for a physics engine. Do not change the underlying mathematical or algorithmic logic. You must apply the following rigorous commenting and structural standards without exception.

Professional, Ahistorical Commenting (NO REFACTORING REFERENCES)
All comments and descriptions must be highly professional and describe the code exactly as it currently exists.
You must be strictly ahistorical. Do not reference the fact that the code was refactored, updated, fixed, or changed.
BAD: "This module strips the previous OpenMP loop dispatcher in favor of a single-thread inline helper function."
GOOD: "This module uses a single-thread inline helper function." Just state what the code is and what it does.

Metaprogramming Distinction (Python vs. C)
You must strictly delineate between documenting the Python generator script and the resulting C code.
Python Comments (#): Use exclusively to explain metaprogramming logic, string manipulation, and Python-side orchestration.
C Comments (// or / /): Use within the generated C strings to explain physics, runtime execution, memory layout, and hardware architecture.

Python-Side Documentation Standards
Module-Level Docstrings: The top-level Python script must begin with a comprehensive, multi-sentence docstring explaining the high-level architecture, purpose, and physics of the module.
Cut the fluff.
Mandatory Sign-off: The module docstring MUST conclude with this exact string on the absolute final line: Author: Dalton J. Moone.
Do not add trailing whitespace, dates, or license fluff after this name.
Function-Level Docstrings: Directly below every Python function definition, you must include a docstring.
Input parameters must be documented using the :param name: description format.
All errors or exceptions raised in the function must be defined using the :raises ErrorType: description format.

C-Side Variable, Struct, and Math Documentation
Structs & Variables: Every new instance of a variable or struct member must have a comment detailing its physical meaning or structural purpose.
No "naked" variables.
Comment Sizing: Use inline C-comments (//) for concise explanations (< 100 characters).
Use a preceding block comment for dense, multi-sentence explanations.

Strict LaTeX Enforcement: All physical variables, tensor indices, and mathematical equations within C comments and the desc string MUST be wrapped in standard LaTeX delimiters (e.g., $p_t$, $\Gamma^a_{bc}$).
Numerical Justification: When documenting an equation, limit the explanation to a brief, single-sentence functional justification of why the numerical method was chosen (e.g., for speed or stability), avoiding dense theoretical derivations or lengthy physics lectures.

C-Side GPU Memory, Data Transfers, and CUDA Kernels
Memory/Transfers: Every device allocation (cudaMalloc), memory copy (cudaMemcpy), or kernel launch (<<<...>>>) must include a detailed comment.
Explicitly state the direction of the transfer (e.g., Host-to-Device) and the hardware justification (e.g., "Pinned memory used to maximize PCIe bandwidth").
Parallelization & Errors: Document CUDA kernels by explicitly stating thread hierarchy logic (e.g., "Thread ID maps to unique photon index").
For error blocks, state if an exit is a fatal unrecoverable error or a soft warning.

C-Side Logical Sectioning & Block Comments
Visually separate long, complex C strings into logical blocks using clear dividers (e.g., // --- EVENT DETECTION & TERMINATION CHECKS ---).
Precede each section with a fluff-free description of the algorithmic steps and explicitly justify why this architectural step occurs at this specific point in the pipeline.

Strict Ordering and Formatting for cfc.register_CFunction
All string variables representing the inputs must be defined sequentially immediately above the function call.
The Master Order: The arguments defined and passed into register_CFunction MUST follow this canonical top-to-bottom sequence: prefunc, includes, desc, cfunc_type, name, params, include_CodeParameters_h, body, postfunc.
However if prefunc or postfunc doesn't exist in the script do not add it or put it in. That will cause bloat.

Example of Strict Ordering:
Notice how all variables passed to register_CFunction are defined immediately above the call, in the exact order they appear in the final generated C script:

Python    # ... [Docstrings, Python logic, and SymPy setup] ...
prefunc = """
#ifdef USE_GPU
__device__ __forceinline__
#endif
"""
includes = ["BHaH_defines.h"]
desc = """@brief Computes the initial temporal component of the 4-momentum (p^0).
Detailed algorithm: Solves the quadratic Hamiltonian constraint p_mu p^mu = 0
specifically for the negative root to enforce physical null photon trajectories
within the SoA architecture."""
name = "p0_reverse"
params = (
    "const double *restrict metric_g4DD, "
    "double *restrict all_photons_f, "
    "const long int num_rays, "
    "const int batch_size, "
    "const long int photon_idx, "
    "const int batch_id, "
    "double *restrict p0_out"
)
# ... [Body string generation logic] ...
body = f"""
{preamble}
{body_math}
{postamble}
"""
# Note: postfunc is omitted if not needed for CUDA specific cleanup
cfc.register_CFunction(
    prefunc=prefunc,
    includes=includes,
    desc=desc,
    name=name,
    params=params,
    body=body,
    include_CodeParameters_h=False,
    postfunc="",
)

The desc String Formatting: The description string MUST be a multi-line, triple-quoted block string.
It must utilize the Doxygen @brief tag and include @param tags explaining the C-function arguments.

Execution:
Review the provided code.
Refactor and comment it strictly adhering to every rule above.
Ensure the physics remains front and center, the hardware logic is explicitly justified, and the metaprogramming boundaries are respected.
"""



Most recent prompt:
""""



I gave a group of LLMs this prompt:

""

I'm currently updating these scripts to work on a GPU. I believe I'm having memory transfer issues. When I run the scripts I get this:

""

[DIAGNOSTIC] Initialization Alignment Check: 217232 out of 250000 rays (86.89%) fail coordinate/placeholder validation.

             Component Breakdown of Failures:

               $t$       (Target: 1000.00) : 217232 rays (86.89%)

               $x$       (Target: 51.00) : 217232 rays (86.89%)

               $y$       (Target: 0.00) : 0 rays (0.00%)

               $z$       (Target: 10.20) : 217232 rays (86.89%)

               $p_t$     (Target: 0.0)  : 0 rays (0.00%)

               $\lambda$ (Target: 0.0)  : 0 rays (0.00%)

""

For these code parameters:

""

numerical_initial_h = 0.1               # (REAL)

camera_pos_x = 51.0                     # (REAL)

camera_pos_y = 0.0                      # (REAL)

camera_pos_z = 10.2                     # (REAL)

window_center_x = 50.0                  # (REAL)

window_center_y = 0.0                   # (REAL)

window_center_z = 10.0                  # (REAL)

window_height = 1.0                     # (REAL)

window_up_vec_x = 0.0                   # (REAL)

window_up_vec_y = 0.0                   # (REAL)

window_up_vec_z = 1.0                   # (REAL)

window_width = 1.0                      # (REAL)

""

Here is another LLM thinks:

""

This diagnostic output is a "smoking gun" that tells us exactly where the initialization is failing. The fact that $y$, $p_t$, and $\lambda$ have 0.00% failure while $t$, $x$, and $z$ have 86.89% failure reveals that this is not a random memory corruption or a thread-indexing issue—it is a parameter mapping failure.



The Core Issue: The "Zero-Value" Coincidence

Look closely at the targets for the components that passed (0.00% failure):



$y$ Target: 0.00 





$p_t$ Target: 0.0 





$\lambda$ Target: 0.0 



These components "passed" only because their target values are exactly zero. In CUDA, when a kernel fails to execute on certain threads or memory is allocated but not hydrated, those memory slots often contain 0.0.



The components that "failed" ($t$, $x$, and $z$) all have non-zero targets:





$t$ Target: 1000.00 (Failed) 





$x$ Target: 51.00 (Failed) 





$z$ Target: 10.20 (Failed) 



Why exactly 86.89%?

Your total ray count is 250,000. Your BUNDLE_CAPACITY is 32,768.



$250,000 / 32,768 \approx 7.63$ batches.

This means you have 7 full batches and one partial remainder batch.

One full batch ($32,768$) out of $250,000$ is exactly 13.11%.

$100\% - 13.11\% = \mathbf{86.89\%}$.

The Conclusion: Your kernel is only successfully initializing the very first batch of 32,768 rays. Every single ray after the first batch is being left as zeroed-out memory.



The Fix: Check the start_idx logic

The issue is likely in your host-side pagination loop. Even though you are looping through the rays, you might be accidentally writing to the same VRAM address (d_f_bundle) but failing to copy the results back to the correct offset in the all_photons_host.f array for subsequent batches.



Check your set_initial_conditions_kernel_KerrSchild_Cartesian call. Specifically, ensure the cudaMemcpy that pulls data back to the host is using the start_idx to offset the destination pointer:



C



// --- 9-STRIDED BRIDGE TRANSFER (DEVICE-TO-HOST) ---for (int m = 0; m < 9; m++) {

  // Ensure 'all_photons->f + (m * num_rays) + start_idx' is the destination

  cudaMemcpy(all_photons->f + (m * num_rays) + start_idx, 

             d_f_bundle + (m * BUNDLE_CAPACITY), 

             sizeof(double) * chunk_size, 

             cudaMemcpyDeviceToHost);

}

If the + start_idx is missing in your Python-generated C code, every batch will overwrite the first 32,768 slots of your host array, leaving the rest of the 250,000 rays as zeros.



Would you like me to look at the cudaMemcpy lines in your generated set_initial_conditions_kernel_KerrSchild_Cartesian.cu to see if the offset math is correct?

""

Please check if you agree with them. If you do please try to find the memory bug. This should be very simple as the d_commondata should never be updated, ever, just drawn from. Thank you

""

I will give you their outputs and I need you to sort their ideas, if multiple LLMs have the same idea please show that. Then I need you to state if you agree with it. Afterwards please proivde all required code blocks to make the updates. PLease be sure to meet all standards set in the commenting standards prompt in the CUDA.txt file. Thank you



outputs:



LLM 1:

""

You’re right to be skeptical. The other LLM is partially correct about the math, but its conclusion is contradictory, and its proposed fix is pointing to an error that doesn't actually exist in your code.

Here is a breakdown of what is actually happening and why the other LLM is off the mark.

The Math is Correct (Exactly One Batch Succeeds)

The other LLM accurately deduced the math behind the 86.89% failure rate. With a total of 250,000 rays and a BUNDLE_CAPACITY of 32,768, your simulation requires 7 full batches and 1 partial batch.

Exactly one batch represents 13.11% of your rays.

That leaves exactly 86.89% of your rays failing.

Because the targets for $y$, $p_t$, and $\lambda$ are 0.0, they artificially "pass" the diagnostic test. The host memory for the remaining 217,232 rays is entirely zeroed out.

Why the Other LLM is Wrong

The LLM contradicts itself. It claims this is a "parameter mapping failure" but then blames a missing + start_idx in your cudaMemcpy loop.

More importantly, your code already has the exact fix it suggested. In the set_initial_conditions_kernel_KerrSchild_Cartesian.cu file you provided, your memory transfer is perfectly strided:

C



cudaMemcpy(all_photons->f + (m * num_rays) + start_idx, d_f_bundle + (m * BUNDLE_CAPACITY), sizeof(double) * chunk_size, cudaMemcpyDeviceToHost);

Your pointer arithmetic (m * num_rays + start_idx) perfectly matches the master SoA host array, and d_f_bundle + (m * BUNDLE_CAPACITY) correctly strides through the VRAM scratchpad. The copy logic is mathematically sound.

Finding the Real Memory Bug

If the cudaMemcpy offsets are correct, but the host memory remains zero for every batch after the first, it means cudaMemcpy is successfully copying exactly what is inside d_f_bundle—which means d_f_bundle is full of zeroes during batches 2 through 8. This happens when a CUDA kernel silently aborts mid-launch, leaving the target memory untouched. Since d_f_bundle was zeroed/unwritten for the second batch, it copied those zeroes back to the host. Here are the two most likely culprits causing your kernel to silently fail after the first batch:

1. The d_commondata Linkage Boundary (Most Likely)

You mentioned that d_commondata should only ever be drawn from. In batch_integrator_numerical.cu, you correctly hydrate the constant memory:

cudaMemcpyToSymbol(d_commondata, commondata, sizeof(commondata_struct));

However, constant memory (__constant__) has internal linkage in CUDA by default. If set_initial_conditions_kernel_KerrSchild_Cartesian.cu is compiled as a separate object file and you are not compiling with Relocatable Device Code (-rdc=true), the initialization kernel is looking at its own separate instance of d_commondata, which is completely uninitialized (filled with zeroes).

If d_commondata.scan_density evaluates to 0 inside the initialization kernel, the following line triggers a hardware-level divide-by-zero:

const int row = i / d_commondata.scan_density;

On an NVIDIA GPU, an integer divide-by-zero doesn't crash the host; it silently terminates the warp. The threads abort, nothing is written to d_f_bundle, and zeroes get copied back to your host array.

2. Grid Dimension Truncation

Check your BHAH_THREADS_IN_X_DIR_DEFAULT macro. If this is not cleanly dividing your chunk size, or if threads_per_block is evaluating in a way that exceeds the hardware maximums for your block dimensions, the kernel will refuse to launch for start_idx > 0. Because your cudaCheckErrors macro is wrapped in an #ifdef DEBUG block, the orchestrator will silently ignore the launch failure and copy the unwritten VRAM back to the host.

Would you like me to show you how to enforce global linkage for d_commondata across your .cu files, or help you temporarily remove the #ifdef DEBUG guard to catch the exact CUDA fault code?

""

LLM 2:

""

Gemini said



I completely agree with the other LLM's diagnosis of the symptom—it was a great catch. The breakdown of the diagnostic output absolutely proves that only the first batch of 32,768 rays is being properly initialized, while the remaining 217,232 rays are left untouched. Because host memory allocated via cudaMallocHost is typically zeroed out by the OS upon mapping, the variables with target values of 0.0 (y, pt

​, λ) "passed" the test, while the non-zero targets (t, x, z) failed.

However, here is where we need to ground this in reality: The + start_idx fix the other LLM suggested is already in the C code you provided.

If you look at the cudaMemcpy block inside set_initial_conditions_kernel_KerrSchild_Cartesian.cu (around line 198), the offset is correctly implemented:

C



// --- 9-STRIDED BRIDGE TRANSFER (DEVICE-TO-HOST) ---for (int m = 0; m < 9; m++) {

  cudaMemcpy(all_photons->f + (m * num_rays) + start_idx, d_f_bundle + (m * BUNDLE_CAPACITY), sizeof(double) * chunk_size, cudaMemcpyDeviceToHost);

}// --- 1-STRIDED BRIDGE TRANSFER (DEVICE-TO-HOST) ---

cudaMemcpy(all_photons->h + start_idx, d_h_bundle, sizeof(double) * chunk_size, cudaMemcpyDeviceToHost);

Because your pointer arithmetic all_photons->f + (m * num_rays) + start_idx is perfectly sound and respects the SoA stride, the memory transfer bug mathematically should not exist in the current codebase.

If you are still seeing this exact 86.89% failure rate when you run the engine, you are almost certainly encountering a build-chain ghost. Here are the two most likely culprits:

1. The Executable Hasn't Been Rebuilt

Since you are using Python metaprogramming (NRPy) to generate these C scripts, it is extremely common for the Python orchestrator to successfully update the .cu files, but the subsequent make command gets skipped, fails silently, or doesn't flag the updated file as "dirty."

The Fix: Run a hard clean (make clean && make) to force the compiler to ingest the newly generated set_initial_conditions_kernel_KerrSchild_Cartesian.cu file that contains the + start_idx fix.

2. Silent Kernel Crash on Batch 2

If you have successfully recompiled and it's still failing, the kernel is likely crashing as soon as start_idx = 32768.

If a CUDA kernel segfaults (e.g., an out-of-bounds access or division by zero), it corrupts the device context. When this happens, all subsequent synchronous calls—including your cudaMemcpyDeviceToHost—will silently abort. Because your execution guard (#ifdef DEBUG) only catches errors if DEBUG is explicitly defined, you might be swallowing a fatal launch error.

The Fix: Temporarily remove the #ifdef DEBUG wrapper around your cudaGetLastError() block (lines 189-196) to force the engine to loudly report if the hardware is halting execution on the second batch.

Would you like me to review your Python generation script to ensure the C strings are being properly flushed to the right directory during the build process?

""



LLM 3:

""

Gemini said



The other LLM is half-right, but ultimately missed the mark because it didn't look closely enough at the actual code you provided.

Here is the breakdown of what is going on, why the other LLM's deduction of the symptoms is brilliant, but why its proposed fix is wrong.

1. The Other LLM is Right About the Symptoms

The other LLM's mathematical deduction is spot on:

You have 250,000 rays.

Your BUNDLE_CAPACITY is 32,768.

Exactly 217,232 rays fail the check.

250,000−217,232=32,768.

Exactly one batch is successfully making it into your host array. The remaining 86.89% of the rays are failing because their memory slots are perfectly 0.0. (They pass the y, pt

​, and λ checks purely because those targets happen to be zero, while t, x, and z have non-zero targets).

When you allocate pinned memory via BHAH_MALLOC_PINNED (which wraps cudaMallocHost), the OS backs it with zero-pages. The host array starts as exactly 0.0. This proves that for batches 2 through 8, the cudaMemcpyDeviceToHost calls are leaving the memory completely untouched.

2. The Other LLM is Wrong About the Fix

The other LLM guessed that you forgot to increment your destination pointer by start_idx. However, looking at the code you provided:

C



cudaMemcpy(all_photons->f + (m * num_rays) + start_idx, d_f_bundle + (m * BUNDLE_CAPACITY), sizeof(double) * chunk_size, cudaMemcpyDeviceToHost);

Your pointer arithmetic is flawless. * all_photons->f + (m * num_rays) correctly jumps to the start of the specific SoA component (e.g., x or y).

+ start_idx correctly offsets the pointer to the exact start of the current batch.

d_f_bundle + (m * BUNDLE_CAPACITY) perfectly respects the VRAM stride limits.

Since your code already does exactly what the other LLM suggested, the bug has to be something else.

3. The Real Bug: A Silent Kernel Crash

If your pointer math is 100% correct (which it is), the only physical reason cudaMemcpy would leave the rest of the array as untouched zeros is if cudaMemcpy is failing to execute for batches > 0.

In CUDA, if a kernel crashes during execution, it throws the entire CUDA context into an unrecoverable error state. Any subsequent CUDA calls (like your cudaMemcpyDeviceToHost) will silently abort and do nothing.

Because your error-catching block inside the pagination loop is wrapped in an #ifdef DEBUG guard:

C



#ifdef DEBUG

    cudaDeviceSynchronize();

    cudaError_t err = cudaGetLastError();

    // ... exit(1)#endif

If you aren't compiling with the DEBUG flag enabled, the kernel is crashing on start_idx = 32768, silently failing, and suppressing the execution of all the following memory transfers.

What to do next:

You are absolutely correct that d_commondata shouldn't be the issue here—if it were uninitialized or corrupted, the first batch would have failed to write the 1000.0 target for t and crashed immediately.

To find the exact culprit:

Force the error trap to trigger by temporarily removing the #ifdef DEBUG guard inside your set_initial_conditions_kernel_KerrSchild_Cartesian.cu pagination loop.

C



    set_initial_conditions_kernel_KerrSchild_Cartesian_gpu<<<blocks_per_grid, threads_per_block>>>(...);

    

    // TEMPORARILY REMOVE #ifdef DEBUG

    cudaDeviceSynchronize();

    cudaError_t err = cudaGetLastError();

    if (err != cudaSuccess) {

      printf("Init Kernel Failed on Batch %d: %s\n", start_idx, cudaGetErrorString(err));

      exit(1);

    }

Once you do this, the engine will explicitly tell you why the device is halting on the second batch. Given that the math inside the kernel is fairly lightweight, double-check that your BHAH_THREADS_IN_X_DIR_DEFAULT macro matches the 256 you hardcoded into your blocks_per_grid calculation. If the block bounds mismatch, threads in higher indices will fault and crash the context.

""



I gave you the Makefile for extra context, the other LLMs didn't get the makefile. 
""""



output:
""
This assessment analyzes the provided source code, the diagnostic output, and the feedback from the previous three models.Comparison of Previous Model FeedbackThe following table summarizes the key ideas and consensus among the models you consulted:IdeaLLM 1LLM 2LLM 3My AssessmentExact Failure Math (32,768 rays succeed)AgreeAgreeAgreeAgreeFix for + start_idx (Claimed it was missing)DisagreeDisagreeDisagreeAgree (Code is correct)Linkage Issue (d_commondata is zeroed)AgreeN/AN/AAgreeSilent Kernel Crash (Due to #ifdef DEBUG)N/AAgreeAgreePossibleGrid Dimension Mismatch (Hardcoded 256)AgreeN/AN/AAgree (Crucial)The Real Memory Bug: The "Shadow Symbol" ProblemI agree with the mathematical deduction that exactly one BUNDLE_CAPACITY (32,768 rays) is succeeding, leaving 217,232 rays (86.89%) uninitialized. While the previous models identified the symptoms, the actual bug is a combination of Symbol Linkage and Grid Math.1. The Constant Memory "Shadow"In batch_integrator_numerical.cu, you call cudaMemcpyToSymbol(d_commondata, ...). However, in CUDA, __constant__ variables have internal linkage by default. Because set_initial_conditions_kernel_KerrSchild_Cartesian.cu is a separate translation unit (compiled to its own .o file), it contains its own "shadow" copy of d_commondata which remains filled with zeros.When the kernel launches, it reads $t_{start} = 0.0$ and $camera\_pos\_x = 0.0$ from its local, unhydrated copy of the struct. This explains why the diagnostic reports failures for any non-zero target.2. The Hardcoded Grid MismatchIn your initialization script, you have a dangerous hardcoded value in the grid calculation:C++dim3 blocks_per_grid((chunk_size + 256 - 1) / 256, 1, 1);
If your BHAH_THREADS_IN_X_DIR_DEFAULT is set to something other than 256 (e.g., 32), your grid will be 8x smaller than it needs to be. This causes most threads in the batch to never launch, leaving the d_f_bundle partially zeroed.Required Code UpdatesTo resolve these issues, we must enforce extern linkage for the global spacetime constants and fix the grid calculation math.Update 1: Global Symbol LinkageModify your global device defines (typically in a header like BHaH_global_device_defines.h) to ensure every translation unit sees the same physical memory address for d_commondata.C++// --- BHaH_global_device_defines.h ---
// Ensure the constant memory struct is visible across all .cu files
extern __constant__ commondata_struct d_commondata;
In your main orchestrator file, define the symbol once:C++// --- batch_integrator_numerical.cu ---
// Physical definition of the constant memory block
__constant__ commondata_struct d_commondata;
Update 2: Fixed Initialization Kernel LaunchUpdate set_initial_conditions_kernel_KerrSchild_Cartesian.cu to eliminate hardcoded block sizes and respect the rdc=true requirements.C++/**
 * @brief Initializes Cartesian starting conditions for photons.
 * * Hardware Justification: Grid dimensions are calculated dynamically based on 
 * threads_per_block.x to prevent thread-starvation on the final execution batch.
 */
void set_initial_conditions_kernel_KerrSchild_Cartesian(const commondata_struct *restrict commondata, long int num_rays,
                                                        PhotonStateSoA *restrict all_photons, double window_center_out[3], double n_x_out[3],
                                                        double n_y_out[3], double n_z_out[3]) {
  // ... [Geometry Setup Logic Remains Unchanged] ...

  for (int start_idx = 0; start_idx < num_rays; start_idx += BUNDLE_CAPACITY) {
    const long int chunk_size = MIN(num_rays - start_idx, (long int)BUNDLE_CAPACITY);

    // Hardware Justification: Using macro-defined thread counts ensures block alignment.
    dim3 threads_per_block(BHAH_THREADS_IN_X_DIR_DEFAULT, 1, 1);
    
    // Algorithmic Step: Calculate blocks based on the actual threads_per_block to ensure 
    // every ray in the chunk_size is assigned to a physical thread.
    dim3 blocks_per_grid((chunk_size + threads_per_block.x - 1) / threads_per_block.x, 1, 1);

    set_initial_conditions_kernel_KerrSchild_Cartesian_gpu<<<blocks_per_grid, threads_per_block>>>(
        num_rays, d_f_bundle, d_h_bundle, nx_0, nx_1, nx_2, ny_0, ny_1, ny_2, start_idx, chunk_size);

    // Algorithmic Step: Catch unrecoverable device faults immediately after kernel execution.
    // This block is active because -DDEBUG is set in the Makefile.
    cudaDeviceSynchronize();
    cudaCheckErrors(cudaKernel, "set_initial_conditions_kernel_KerrSchild_Cartesian_gpu failure");

    // --- 9-STRIDED BRIDGE TRANSFER (DEVICE-TO-HOST) ---
    for (int m = 0; m < 9; m++) {
      // Hardware Justification: Destination offset + start_idx ensures data is scattered 
      // back to the correct global ray index in the master SoA.
      cudaMemcpy(all_photons->f + (m * num_rays) + start_idx, d_f_bundle + (m * BUNDLE_CAPACITY), 
                 sizeof(double) * chunk_size, cudaMemcpyDeviceToHost);
    }
    
    cudaMemcpy(all_photons->h + start_idx, d_h_bundle, sizeof(double) * chunk_size, cudaMemcpyDeviceToHost);
  }
  // Author: Dalton J. Moone.
}
Would you like me to generate a diagnostic script to verify that d_commondata holds the correct values once inside the GPU kernel?
""

